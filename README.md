# Duy Nguyen

Duy Nguyen's personal website.

## Local development

```bash
bundle config set --local path vendor/bundle
bundle install
bundle exec jekyll serve
```

The site is deployed through the GitHub Actions workflow in `.github/workflows/deploy.yml`.
